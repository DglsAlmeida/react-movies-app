import {
  HeaderContent,
  HeaderDescription,
  HeaderWrapper
} from './header-styles'

export const Header = () => {
  return (
    <HeaderWrapper>
      <HeaderContent>
        <HeaderDescription>Movie App</HeaderDescription>
      </HeaderContent>
    </HeaderWrapper>
  )
}
